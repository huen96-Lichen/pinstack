# PinStack AI 管家 v1 端到端验收清单（最终轮）

- 验收目标：对齐《PinStack AI 管家 v1 PRD + UI 重设计方案》
- 验收日期：2026-04-18
- 验收分支：本地工作区（/Volumes/White Atlas/03_Projects/Screen Pin）
- 验收方式：代码链路核对 + 自动化测试 + 启动联调日志

## A. 产品主链路验收（PRD 核心闭环）

- [x] 统一入口可发起 AI 任务（AI 管家中心 / AI Hub）。
- [x] 任务可统一进入 orchestrator（`ai.orchestrate.run`）并返回统一结构结果。
- [x] 6 类任务入口已具备并挂到统一执行链路（整理、摘要、元数据、Markdown、正式文稿、打开 VK）。
- [x] 默认支持本地优先执行（local-first），并可按策略升级。
- [x] 支持高质量模式（云端优先）和降级策略（fallback）控制。
- [x] 任务结果支持统一回写目标标识（PinStack/VaultKeeper/文档链路字段）。

## B. 信息架构与 UI 重设计验收

- [x] AI 页面已采用“总览 + 主任务 + 下部详情”的中枢化结构方向。
- [x] 聊天能力下沉为次级能力，不再作为唯一主视觉。
- [x] 模型与策略信息从主操作区中分层收纳，避免技术信息压过任务入口。
- [x] 新增“打开 AI 设置”直达链路。

## C. 设置分层验收（本轮重点）

- [x] 普通设置与 AI 设置已分区（顶部分段切换）。
- [x] AI 设置新增“基础模式 / 专家模式”双层结构。
- [x] 基础模式提供“3 步上手”引导。
- [x] 基础模式提供“快速模型搭配”（本地/云端首选 + 模板 + 一键策略）。
- [x] 专家模式保留完整高级能力（Provider/API Key/Base URL/Model ID/高级开关）。

## D. 模型搭配与策略验收

- [x] 已支持 `preferredLocalModelId` 与 `preferredCloudModelId` 持久化。
- [x] 已提供 3 个模型搭配模板：极速本地 / 平衡产出 / 高质量写作。
- [x] 已提供 3 个策略快捷应用：本地优先 / 平衡 / 高质量。
- [x] 策略切换后可同步刷新默认 provider / defaultModelId / fallback。

## E. 快捷键治理验收

- [x] 可配置快捷键已扩展为 7 项。
- [x] 设置层（主进程）具备统一去重与自动交换冲突能力。
- [x] 启动日志显示 7 项快捷键全部成功注册（本地联调）。
- [x] 快捷键相关单元测试通过（冲突交换、去重修复、顺序更新唯一性）。

## F. 回归与质量门禁

- [x] `npm run typecheck` 通过。
- [x] `npm run test` 通过（154/154）。
- [x] `npm run dev` 启动成功，主窗口正常创建，AI 与快捷键链路日志正常。

## G. 验收证据摘要

- 自动化：
  - `typecheck` 0 错误。
  - `test` 154 通过 / 0 失败。
- 启动日志关键点：
  - Vite/Electron 启动完成。
  - `[shortcuts] registration result` 中 7 项 `registered: true`。
  - Dashboard 窗口创建成功。

## H. 剩余风险（发布前建议）

- [ ] 真实用户操作路径的人工 UI 验收（基础/专家切换、快速模型搭配、策略切换）建议在产品包体下再做一轮。
- [ ] 与真实云端密钥/模型的联调仍建议在目标环境完成最终回归。

## 结论

- [x] AI 管家 v1 当前版本达到“可交付验收”状态。
- [x] 本轮目标（基础/专家模式 + PRD 最后一轮清单验收）已完成。
