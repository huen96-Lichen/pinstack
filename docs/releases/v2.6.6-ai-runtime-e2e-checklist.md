# PinStack AI 运行层深改验收清单（v2.6.6）

- 验收日期：2026-04-18
- 验收范围：AI 执行核心、密钥治理、路由统一、诊断可观测、兼容性回归
- 验收方式：代码链路核对 + 自动化测试 + 本地启动联调

## A. 执行核心（高质量优先）

- [x] 云端执行已替换占位路径，支持真实请求与结构化返回。
- [x] 默认策略可进入高质量模式（云端优先）。
- [x] 本地回退仅在显式允许时触发，不再静默假成功。
- [x] 错误码映射覆盖鉴权/限流/超时/模型不可用/网络不可达/运行未就绪。

## B. 密钥与配置治理

- [x] API Key 已切换到 macOS Keychain 存储。
- [x] 设置写入链路拦截并清理 `cloudApiKey` 明文。
- [x] 启动时支持旧明文自动迁移到 Keychain。
- [x] 设置文件落盘后不包含云端明文 Key。

## C. 路由与执行一致性

- [x] `ai.orchestrate.run` 使用统一 `taskRouter` 决策 route。
- [x] 6 类任务输出目标由统一配置提供，不在分支里散落硬编码。
- [x] route 返回增强字段包含 `timeoutMs/retryLimit/outputTarget`。
- [x] AI Hub 与独立聊天执行语义一致（共享系统提示/上下文拼装逻辑）。

## D. 可观测性与诊断

- [x] telemetry 事件可见：`ai.task.started/completed/failed`。
- [x] telemetry 事件可见：`ai.route.selected`、`ai.fallback.triggered`、`ai.latency.bucket`。
- [x] 新增 `ai.diagnostics.snapshot`，可回传 provider/model/timeout/fallback/lastErrorCode。
- [x] 设置页专家模式可刷新并查看诊断快照。

## E. IPC 与兼容性

- [x] 保留 `ai.healthCheck` 既有通道。
- [x] 新增兼容别名 `ai.health`。
- [x] 新增 `ai.config.migrateSecrets` 一次性迁移入口。
- [x] 新增 `ai.diagnostics.snapshot` 排障入口。

## F. 自动化与启动回归

- [x] `npm run typecheck` 通过。
- [x] `npm run test` 通过（154/154）。
- [x] `npm run dev` 可启动并保持主链路可用。

## G. 手工验收建议（发布前）

- [ ] 在真实云端凭据下复测 4 类场景：成功 / 网络失败 / Key 无效 / 限流。
- [ ] 在 UI 中完成一次“基础模式 3 步配置 -> 执行任务 -> 回写结果”的全链路录屏验收。
- [ ] 重启应用后核验：密钥状态、路由策略状态、诊断快照字段一致。
